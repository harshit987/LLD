// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        /**
         * object:
         * 1. ParkingSlot - different types of slots (two wheeler, one wheeler)
         * 2. ParkingLotService - entry and exit functionality
         * 3. ParkingLot - collection of slots - crud on parking slot
         * 3. SlotAllocationStrategy
         * 4. PaymentGateway - multiple options(credit/debit card, cash, UPI) - remaining everything else done
         * 5. EntryGateService - sub service of ParkingLotService
         * 5. ExitGateService - sub service of ParkingLotService
         */
    }
}